# Shared Schemas

This package will hold shared contracts used by:

- the extension
- the control plane
- future admin console clients
- tests and contract validation

The package currently includes:

- schema versioning constants
- TypeScript interfaces
- JSON Schema starter files

These are scaffolds and should be treated as the authoritative place to evolve contracts from the architecture docs.
